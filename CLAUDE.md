# ФАЄРВОЛ (seriousdoom) — контекст для Claude

Воксельный ретро-шутер на three.js: волны монстров, одна арена, очки за
волну. Меню/подача — Doom 1993, устройство волн и твари — Serious Sam.
Отделён от VoxEvasion (там он живёт как мини-игра «Фриланс — отбить DDoS»
в src/minigames; сюда портирован и развивается независимо).

Язык общения с Олегом — русский/украинский. Комментарии в коде — русские,
объясняют «почему», а не «что». Оле́г сам плейтестит и описывает баги;
не гонять долгие проверки — tsc + build + короткое «что проверить».

## Запуск и отладка

```bash
npm run dev          # vite на http://localhost:5180/
npx tsc --noEmit     # типы
npx vite build       # прод-сборка (base './' — готово для itch)
```

- `/assets.html` — витрина всех моделей и звуков (общие модули с игрой)
- Читы: на титуле IDCLEV+2 цифры — прыжок на волну; в адресе `?wave=16`,
  `?perf=1` (счётчик F3), `?edit=1` (сразу в редактор), `?lang=uk|en` (язык
  интерфейса на один запуск, в fw_lang НЕ пишется)
- Съёмка промо: `?bot=1` (автопилот — сам целится, кружит, стреляет, бегает
  за пикапами), `?god=1` (урон только визуально: вспышка, лицо, крик),
  `?music=0` (дорожки переключаются, но молча), `?sfxlog=1` (журнал звуков в
  `window.__sfxLog.events` + белые кадры-синхромаркер в начале, ≥120 мс),
  `?spawn=x,z,yaw` (yaw в градусах; точка внутри пилона — флаг игнорируется)
- Бенчи в корне: `wave20test.html`, `audiotest.html` (кнопкой), в
  VoxEvasion-minigames ещё `lagtest.html`/`insttest.html`. В сборку не
  попадают (vite собирает только index+assets).
- ВАЖНО про браузерную панель Claude: скрытая вкладка замораживает rAF —
  игра «стоит», время идёт только в момент скриншота. Синтетические
  keydown/mousedown работают; edge-детекция клавиш (стрелки в меню) —
  ненадёжна. Звук WebAudio НЕ замирает: выходи из игры после проверки,
  иначе музыка играет фоном.

## Карта кода

- `src/games/doom.ts` (~3000 строк) — ВСЯ игра: арена, монстры, оружие,
  волны, сложности, HUD, редактор арен. Точки входа по коментарям-секциям.
- `src/games/i18n.ts` — ВСЕ строки интерфейса на uk/en одним плоским
  словарём, makeT(lang) и hasStr. Новый текст на экране заводится здесь.
- `src/games/doomModels.ts` — модели (монстры/оружие/пикапы/структуры),
  ArenaDef + DEFAULT_ARENA + BUILTIN_ARENAS + validateArena, bakeStatic,
  EmberCloud/DustCloud (InstancedMesh-частицы), createPentagram.
- `src/core/three3d.ts` — рантайм: PerspectiveCamera (persp:true),
  HUD-канвас поверх WebGL, ctx.perf (замер кадра), ctx.compile (прогрев).
- `src/core/audioDoom.ts` — сэмплы /sfx, позиционный звук (затухание,
  панорама, ФНЧ вдали), sfx.loop() для непрерывного воя камикадзе.
- `src/core/musicDirector.ts` — main/middle/end/chill с кроссфейдами.
- `src/main.ts` + `index.html` — только запуск игры на всю страницу.
  Заставки перед игрой НЕТ: главный экран — титул внутри самой игры, оттуда
  же и новый забег. Выхода из игры нет (в браузере некуда), Escape уводит на
  титул вторым нажатием — первое браузер тратит на возврат курсора.

## ДВА ЖЕЛЕЗНЫХ ПРАВИЛА ОТРИСОВКИ (добыты замерами, не нарушать)

1. **Число источников света в сцене не меняется НИКОГДА.** Подключение/
   снятие PointLight = пересборка программ всех материалов: замер давал
   13 мс средний кадр вместо 1.8 и провал 167 мс. Всё световое живёт в
   пулах и гасится intensity=0 (файерболы, ракеты, глаза меню, ствол).
2. **Частицы и статика — минимум вызовов.** Искры/гибы/пыль —
   InstancedMesh (EmberCloud аддитивный, DustCloud с альфой через
   onBeforeCompile). Статика арены запекается bakeStatic (вершинные
   цвета, общий BAKED_MAT с userData.shared — dispose его щадит).
   Монстры: геометрия кеширована на вариант (тип×ветеран), спавн = 2 меша
   + 2 клона материала; вспышка попадания — униформа uFlash, не перекраска.

## Ключевые системы

- **ArenaDef** — арена как данные: size (16..40, шаг 2), sky (hell/dusk/
  day/void), ground (checker/sand/sand_road/stone/cobble), pillars
  (kind: block/obelisk/wall/pyramid/rubble/column/steps/rock/statue/dais,
  rot 0/45/90/135), torches, seals (мин. 2!), pickups, start.
  Хранилище: localStorage fw_arenas {имя:def}, fw_arena_draft, fw_arena_use
  ('default'|'b:N'|'draft'|'u:имя'). validateArena зажимает всё чужое.
  При добавлении нового kind НЕ ЗАБЫТЬ kinds0-белый список в validateArena
  (уже наступали: элементы молча превращались в block).
- **dais (платформа)** — ПРОХОДИМАЯ: groundY(x,z) с пандусом ~1.1 м по
  краю. Высоту знают: камера (EYE+pY), ракеты, файерболы (пуск от m.y,
  попадание по EYE+pY), наземные монстры (m.y=groundY; анимационный
  сдвиг grp.position.y СБРАСЫВАЕТСЯ в начале кадра — иначе копится и
  твари улетают в небо, уже чинили), пикапы, печати, маркер старта.
  Монстры на подъёме замедляются ×0.72.
- **Разгон рогача/прыжок скакуна**: направление заморожено; застревание
  у внутренних стен детектится по факту (<40% намеченного пути за кадр →
  срыв разгона). Не убирать — иначе стоят у стен арена-гейта. Три условия
  вокруг этой проверки добыты замерами в Godot-порте, их тоже не трогать:
  срыв считается ТОЛЬКО у того, кто в этом кадре бежал (`state0`, иначе кадр
  «завод кончился → разгон» обрывает сам себя); рогач заводится только при
  остывшем `atkCd`, а срыв даёт 1.2 с обхода вбок (иначе он бесконечно
  заводится напротив того же пилона — 23 с заводки из 30); разведение тварей
  идёт ОТДЕЛЬНЫМ проходом после хода всех, иначе толчок соседа попадает
  в замер пути и толпа ложно срывает разгон.
- **Обход препятствий**: пришедшая в пилон в лоб тварь упирается насмерть —
  выталкивание симметрично. Сторона обхода (`skirtT`/`skirtN`) выбирается
  один раз на 1.2 с и своя у каждого (по `ph`). Пересчитывать её каждый кадр
  НЕЛЬЗЯ: твари начинают колебаться у пилона и доезжают хуже, чем вовсе без
  обхода (проверено замером).
- **Заряд камикадзе бьёт по СВОИМ** — и когда он добежал, и когда его убили
  (`killMon` → `boomBlast`). Урон `BOOM_SPLASH` растёт вместе с живучестью
  тварей, поэтому целый бомбист взрыв соседа переживает всегда и цепь сама не
  расходится; глубина ограничена `blastDepth`. Игрока осколки от
  застреленного НЕ задевают. Из-за цепочек смертей перебор тварей в бою идёт
  по КОПИЯМ списка (`[...mons]`).
- **HUD-масштаб смотрит и на ширину**: `hs() = min(H/720, W/560)` с полом
  0.55. Без ширины портретный телефон давил счётчик волны в панель оружия;
  пол держит тач-мишени нажимаемыми на ландшафтном телефоне. Подсказка
  титула привязана к концу меню, не к 0.82H (шесть пунктов доставали).
- **HUD рисуется в координатах базовой высоты 720** и растягивается одним
  преобразованием канваса (`hs()`/`HW()`/`HH()`), иначе на большом экране всё
  мельчает: размеры заданы в пикселях и упираются в жёсткие потолки. Всё, что
  сверяет указатель с вёрсткой, обязано делить координаты на `hs()` — `pv()`.
- **Поворот структур** 45°: визуал — повёрнутые подгруппы (bakeStatic
  печёт по matrixWorld), коллизии/LOS — по ОПИСАННОМУ прямоугольнику
  (ex/ez; на 45° шире визуала — осознанный компромисс).
- **Рекорд** (`fw_best`): свой на каждую сложность — множитель очков у хардкора
  почти вдвое больше, общий рекорд принадлежал бы только ему. Забеги с прыжком
  по волнам (`?wave=`, IDCLEV) помечаются `cheated` и в рекорд НЕ идут: первая
  же зачищенная волна дала бы очки за все пропущенные. Пишется и при смерти,
  и при уходе на титул — очки-то заработаны.
- **Язык** (`fw_lang`): uk — базовый, en — для itch; пункт «МОВА/LANGUAGE» в
  титуле, автоопределение по navigator.language (uk/ru/be → uk). Строки НЕ
  кешируются в переменные: HUD рисуется каждый кадр, смена языка = новый `t`.
  Название «ФАЄРВОЛ» не переводится никогда (в en под ним мелко FIREWALL).
  Локализованы ярлыки арен, но НЕ ключи хранилища и не имена своих арен;
  сложности берут название по своему `key` (`diff.norm` и т.д.). Счётчик F3 и
  MDEFS/WEAPONS.name (нигде не рисуются) оставлены по-русски намеренно.
- **Производительность у игроков**: три слоя защиты. (1) АДАПТИВНОЕ
  РАЗРЕШЕНИЕ в three3d.ts — лестница pixelRatio 2→0.85 по замеру кадра
  (вниз при >22 мс дольше 2.5 с, вверх при <12 мс дольше 8 с, после спуска
  подъём остывает 20 с); HUD-канвас всегда в родном разрешении — текст не
  мылится. Тач стартует с капа 1.5. Текущий ×dpr виден в F3. (2) Пункт
  «ЯКІСТЬ» в титуле (fw_quality): НИЗЬКА = кап 1 + половина пулов частиц +
  без флейра сразу, MSAA отключается при следующем запуске (main.ts читает
  ключ до создания рендерера). (3) Прогрев шейдеров на титуле — был всегда.
  Отсутствие WebGL даёт текстовое сообщение, не чёрный экран.
- **Сложности** (DIFFS): звичайна/важка/хардкор — HP тварей НЕ трогаем,
  растут урон/плотность/потолок (26/32/44), лечение скупеет. Выбор в
  титуле, localStorage fw_diff.
- **Оружие**: пистолет/дробовик 3в/пулемёт 6в/ракетница — на 16-й волне
  ЛЕЖИТ НА АРЕНЕ (парит, светится). Ракета: 90 прямого + 70 осколков
  (спад от КРАЯ твари до 5.5 м), самоурон ×0.35 через hurt(). Выдача
  стволов — `wave >= N` (не ===, ломалось при прыжке IDCLEV).
- **Спавн**: печать заряжается 2 с (телеграф, голограмма+искры), потом
  монстр. Очередь pending; wave-clear ждёт и её. У камикадзе непрерывный
  вой sfx.loop, обрывается в gibify.

## Права (для публикации)

Модели/геометрия — свои. Звуки — CC0 (Kenney, OGA) + свои, вой камикадзе
из orr.wav Олега. МУЗЫКА (public/music) и ШРИФТ HalfBoldPixel7 — лицензия
НЕ подтверждена, до коммерческого релиза заменить/подтвердить. Детали в
ASSETS.md. Папку sounds/placeholder из соседних репо НЕ использовать.

## Известные компромиссы (не баги)

- хитскан игрока бьёт СКВОЗЬ пилоны (укрытия работают только против
  механоида) — осознанно, менять только по просьбе Олега
- флейр солнца не проверяет заслонение пирамидой
- гибы на платформе визуально проседают в неё (падают до y=0.08)
- 45°-коллизия шире визуала (см. выше)
- iPhone/Ogg: в основном VoxEvasion есть public/audio-check.html для
  проверки; здесь музыка в ogg+m4a, sfx в wav/ogg

## Соседние репозитории

- `../VoxEvasion` — основная игра (git@github.com:AlekVoyan/Voxel-Evasion),
  дум там в src/minigames, звуки в public/sfx/doom. Изменения тут НЕ
  синхронизируются туда автоматически — порт руками, если Олег попросит.
- `../VoxEvasion-minigames` — песочница 16 мини-игр, исходник этого кода
  (коммит есть, remote ещё не назначен).
